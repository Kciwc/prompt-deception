{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx prisma db push && cd client && npm install && npm run build && cd .. && node server/index.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120
  }
}
